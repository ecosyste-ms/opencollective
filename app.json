{
  "cron": [
    {
      "command": "bundle exec rake collectives:sync",
      "schedule": "*/30 * * * *"
    },
    {
      "command": "bundle exec rake projects:sync",
      "schedule": "*/30 * * * *"
    },
    {
      "command": "bundle exec rake collectives:discover",
      "schedule": "0 0 * * *"
    }
  ]
}