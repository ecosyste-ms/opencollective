<% @meta_title = "#{@project} advisories" %>
<% @meta_description = "Security Advisories for #{@project}" %>

<div class="container-md">

  <%= render 'nav' %>

  <h1>
    <%= @project.to_s %>
  </h1>

  <p>
    <%= @project.description %> <br/>
    <%= link_to @project.url, @project.url %>
  </p>

  <%= render partial: 'project_nav', locals: {active: 'advisories'} %>

  <% if @pagy.count > 0 %>
    <%= render @advisories %>
    <%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>  
  <% else %>
    <p>
      No advisories found.
    </p>
  <% end %>
</div>